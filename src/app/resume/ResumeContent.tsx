import { FiGithub, FiLinkedin, FiYoutube } from "react-icons/fi";

const ResumeContent = () => {
  return null; // This component is not actually used, all content is in the main Resume component
};

export default ResumeContent;
